import styled from 'styled-components';

export const Container = styled.main`
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    width: 100%;
    max-height: 100vh;
    justify-content: space-between;
`
export const ShowComics = styled.section`
    margin-left: 30px;

`
